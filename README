Command line tool to assign users to issues based on specified regular expressions.

Usage: __main__.py [OPTIONS] REPOSLUG

  CLI tool for automatic issue assigning of GitHub issues

Options:
  -s, --strategy [append|set|change]
                                  How to handle assignment collisions.
                                  [default: append]
  -d, --dry-run                   Run without making any changes.
  -a, --config-auth FILENAME      File with authorization configuration.
                                  [required]
  -r, --config-rules FILENAME     File with assignment rules configuration.
                                  [required]
  --help                          Show this message and exit.

  Contains a flask application which after deployment can be setup as a webhook to automatically assign people to new and edited issues
